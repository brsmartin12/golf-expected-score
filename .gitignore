# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Environment
.env

# Node (arrives in step 3)
node_modules/
dist/

# OS
.DS_Store

# Database dumps. These hold real scores, and a restore is only ever wanted from
# a file you deliberately kept -- not one that rode into the repo by accident.
backup-*.sql
*.dump
